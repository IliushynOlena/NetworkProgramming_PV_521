<Window x:Class="_03_SMTP_Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_03_SMTP_Client"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <DockPanel>
        <Grid DockPanel.Dock="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition  Height="Auto"/>
                <RowDefinition  Height="Auto"/>
            </Grid.RowDefinitions>
            <Label>From</Label>
            <Label Grid.Row="1">To</Label>
            <Label Grid.Row="2">Subject</Label>
            <TextBox x:Name="fromTb" Grid.Column="1"></TextBox>
            <TextBox x:Name="toTb" Grid.Column="1" Grid.Row="1"></TextBox>
            <TextBox x:Name="subjectTb" Grid.Column="1" Grid.Row="2"></TextBox>
        </Grid>
        <Button DockPanel.Dock="Bottom" Click="Button_Click">Send</Button>
        <TextBox DockPanel.Dock="Top" x:Name="messageTb"></TextBox>
        <ListBox SelectionChanged="ListBox_SelectionChanged" ItemsSource="{Binding}">
           
        </ListBox>
    </DockPanel>
</Window>
